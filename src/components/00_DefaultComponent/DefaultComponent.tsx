import styles from "./DefaultComponent.module.scss";

type ComponentProps = {};

const DefaultComponent = ({}: ComponentProps) => {
  // VARIABLES ----------------------
  // CONDITIONS ---------------------
  // FUNCTIONS ----------------------
  // VARIABLES ----------------------
  return <div className={styles.DefaultComponent}>Default component</div>;
};

export default DefaultComponent;
